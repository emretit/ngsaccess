bin/
obj/
*.user
.vs/

# Hikvision vendor DLL'leri — lisans nedeniyle commit edilmez (SDK'dan kopyalanır).
# README'deki listeye göre hikvision-sdk/win-x64/ klasörüne elle yerleştirilir.
hikvision-sdk/win-x64/*.dll
hikvision-sdk/win-x64/HCNetSDKCom/

# Yerel köprü ayarı (Convex URL + bridge token) — buluta gitmez.
bridge.json
# Eski model artığı (artık kullanılmıyor; güvenlik için yine de ignore).
panels.json

# Gömülü ngsplus arayüzü — `npm run build` çıktısı; commit edilmez, build sırasında üretilir.
wwwroot/
